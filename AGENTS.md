# Overview of What We Are Building

**We are building a Telegram Bot Hosting platform that hosts PHP files, allowing users to manage their files, upload new files, and more through the bot interface.**

* Our reference is the `reference` folder, which already contains a PHP hosting bot. However, we only take inspiration from its core ideas and the important parts. Do not start reviewing it unless explicitly asked to do so.

# Code Preferences

**It is strongly preferred that if any single file exceeds 1200 lines of code, you must inform the user that it should be split into a better and more scalable architecture to avoid file bloat, making restructuring or adding new features difficult in the future.**

**When adding a feature or fixing an issue, you must carefully review all related parts of the system and fully understand the connected areas to avoid introducing any bugs or side effects.**

# Work Environment

**This environment is only for writing code. You should not download or install any PHP-related dependencies here. If you only want to test whether Python code works, you may do so because Python is already installed.**

# Workflow

**When working on any specific part or making any modifications, you must strongly ensure that you create a commit for the changes (important). If you are writing documentation files, you must place them inside their own dedicated folder within the `docs/` directory so they remain organized and easy to find as the number of files grows.**


You must pay close attention to details, quality, and focusing on the requested tasks. Do not make major decisions without asking the user first, and pay strong attention to the workflow and development process.
